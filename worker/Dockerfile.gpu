ARG BASE_IMAGE=clipflow-worker-base:latest
FROM ${BASE_IMAGE}

USER root

WORKDIR /app

COPY app /app/app
COPY assets /app/assets

RUN chown -R appuser:appuser /app/app /app/assets

USER appuser

CMD ["python", "-m", "app.main"]
