"""Trend agent (placeholder).

TODO(Phase 8): score relevance/trend for discovered candidates (feeds VideoCandidate.scores).
No logic yet — discovery/intelligence is a later phase.
"""
from __future__ import annotations

from app.agents.base_agent import BaseAgent


class TrendAgent(BaseAgent):
    name = "trend"
    # TODO(Phase 8): implement trend/relevance scoring.
