"""Metadata agent (placeholder).

TODO(Phase 5+): generate title / description / hashtags / thumbnail prompt for publication.
Will run through the provider router. No logic yet.
"""
from __future__ import annotations

from app.agents.base_agent import BaseAgent


class MetadataAgent(BaseAgent):
    name = "metadata"
    # TODO(Phase 5+): implement metadata generation.
