"""Pipeline graph definition (placeholder).

TODO(Phase 5+): declare nodes/edges for the autonomous pipeline graph (discover → select →
download → transcribe → analyze → generate → render → publish), mapped onto the existing
PipelineState machine. No logic yet.
"""
from __future__ import annotations

# TODO(Phase 5+): define PIPELINE_GRAPH nodes and transitions here.
PIPELINE_GRAPH: dict = {}
